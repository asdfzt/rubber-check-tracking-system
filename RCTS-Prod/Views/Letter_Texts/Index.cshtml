@model IEnumerable<RCTS_Prod.Models.Letter_Text>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Letter_Path)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Letter_Path)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Letter_TextID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Letter_TextID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Letter_TextID })
        </td>
    </tr>
}

</table>
